#include <bits/stdc++.h>
using namespace std;

int main()
{
    double length, breadth;
    cout << "Enter length: "; cin >> length; //reading length from user
    cout << "Enter breadth: "; cin >> breadth; //reading breadth from user
    cout << "Area of " << length << " x " << breadth << " rectangle = " << length * breadth;
}