#include <bits/stdc++.h>
using namespace std;

class Node {
    public:
        Node *previous;
        int data;
        Node *next;

        Node (int value) {
            data = value;
            previous = nullptr;
            next = nullptr;
        }
};

int main() {
    
}